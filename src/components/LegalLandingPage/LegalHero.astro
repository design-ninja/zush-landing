---
import { Image } from 'astro:assets';
import heroPhoto from '@/assets/landing/legal-hero.webp';
import DownloadButton from '@/components/DownloadButton';
import Heading from '@/components/Heading';
import Text from '@/components/Text';
import { LEGAL_HERO } from '@/data/legalLanding';
import styles from './LegalHero.module.scss';
---

<section class={styles.Hero} aria-labelledby="legal-hero-title">
  <div class={styles.Hero__Media} aria-hidden="true">
    <Image
      class={styles.Hero__Photo}
      src={heroPhoto}
      alt={LEGAL_HERO.photo.alt}
      widths={[640, 960, 1216, 1408]}
      sizes="100vw"
      loading="eager"
      decoding="async"
    />
  </div>
  <div class={styles.Hero__Scrim} aria-hidden="true"></div>

  <div class={styles.Hero__Container}>
    <div class={styles.Hero__Intro}>
      <p class={styles.Hero__Eyebrow}>{LEGAL_HERO.eyebrow}</p>

      <Heading as="h1" align="center" id="legal-hero-title" className={styles.Hero__Title}>
        {LEGAL_HERO.titleLead}<span class="brand-accent-text">{LEGAL_HERO.titleAccent}</span>
      </Heading>

      <Text as="p" size="lg" color="subtle" align="center" className={styles.Hero__Subtitle}>
        {LEGAL_HERO.subtitle}
      </Text>

      <div class={styles.Hero__Actions}>
        <div class={styles.Hero__Buttons}>
          <DownloadButton
            client:visible
            source="hero"
            size="lg"
            forceOS="mac"
            showDropdown={false}
            label="Download for Mac"
          />
          <DownloadButton
            client:visible
            source="hero"
            size="lg"
            forceOS="windows"
            showDropdown={false}
            label="Download for Windows"
          />
        </div>

        <ul class={styles.Hero__TrustRow} aria-label="Controls for legal document workflows">
          {LEGAL_HERO.trustLine.map((item) => <li>{item}</li>)}
        </ul>
      </div>
    </div>
  </div>
</section>
