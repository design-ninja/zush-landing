/**
 * Desktop width of the header pill. `default` lines it up with the 60rem
 * content container, `wide` with the 72rem blog container, `container` with
 * the 80rem page container the feature-landing heroes use.
 */
export type HeaderWidth = 'default' | 'wide' | 'container';
