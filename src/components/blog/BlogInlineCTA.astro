---
import BlogCTA from '@/components/blog/BlogCTA.astro';
import type { BlogPlatform } from '@/data/blogSchema';

interface Props {
  platform?: BlogPlatform;
}

const { platform = 'general' } = Astro.props;
---

<BlogCTA placement='inline' platform={platform} />
