---
import headingStyles from '@/components/Heading/Heading.module.scss';

const { class: className = '', ...props } = Astro.props;
---

<h3
  {...props}
  class={[headingStyles.Heading, headingStyles.Heading_h3, className]
    .filter(Boolean)
    .join(' ')}
>
  <slot />
</h3>
