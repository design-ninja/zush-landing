---
import BaseLayout from '@/layouts/BaseLayout.astro';
import RecoverView from '@/views/Recover';
import { getSeoForPath } from '@/seo/config';

const seo = getSeoForPath('/recover');
---

<BaseLayout seo={seo}>
  <RecoverView client:load />
</BaseLayout>
