---
import BaseLayout from '@/layouts/BaseLayout.astro';
import FeatureLandingPageShell from '@/components/FeatureLandingPage/FeatureLandingPageShell.astro';
import { getFeaturePageProps } from '@/utils/featurePageProps';
import AutoRenameFilesView from '@/views/FeaturePages/AutoRenameFiles';
import { getSeoForPath } from '@/seo/config';
import { buildFeatureBreadcrumbJsonLd } from '@/utils/jsonLd';

const seo = getSeoForPath('/auto-rename-files');
const featurePage = getFeaturePageProps(AutoRenameFilesView);
---

<BaseLayout seo={seo} jsonLd={[featurePage.jsonLd, buildFeatureBreadcrumbJsonLd(featurePage.h1, '/auto-rename-files')]}>
  <FeatureLandingPageShell {...featurePage} />
</BaseLayout>
