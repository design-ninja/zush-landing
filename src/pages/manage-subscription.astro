---
import BaseLayout from '@/layouts/BaseLayout.astro';
import ManageSubscription from '@/views/ManageSubscription';
import { getSeoForPath } from '@/seo/config';

const seo = getSeoForPath('/manage-subscription');
---

<BaseLayout seo={seo}>
  <ManageSubscription client:load />
</BaseLayout>
